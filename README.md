# Rework completo (v2)

Rama de reescritura completa del e-commerce.

Este branch utiliza la versión más moderna de **Next.js**, con **TypeScript** y una arquitectura optimizada (App Router, Server Actions, cacheo y tipado estricto).

⚠️ No corresponde a la versión actualmente en producción.
